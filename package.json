{
  "name": "database-operations",
  "version": "1.0.0",
  "description": "A tool that allows you to run operations on a database and export its results without having the need to spin up your own MySQL, MariaDB, or Postgres Database.",
  "main": "process-database.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@faker-js/faker": "^9.5.0",
    "mysql2": "^3.12.0",
    "sequelize": "^6.37.5"
  }
}
